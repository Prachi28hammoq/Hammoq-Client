apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: hammoq-client-dev
  name: hammoq-client-dev
  namespace: default
  selfLink: /api/v1/namespaces/default/services/hammoq-client-dev
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 3000
  selector:
    app: hammoq-client-dev
  sessionAffinity: None
  type: LoadBalancer
status:
  loadBalancer: {}